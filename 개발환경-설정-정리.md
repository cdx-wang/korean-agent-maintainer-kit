# 개발환경 설정 정리

이 문서는 이 저장소(`korean-agent-maintainer-kit`)의 개발환경을 처음 세팅하고, 실제로 잘 작동하는지 확인한 과정을 비개발자도 이해할 수 있게 정리한 기록이야. 각 항목에는 실제로 쓴 명령어와 결과도 함께 적어뒀어.

## 1. 이 프로젝트가 뭐야

- 터미널에 명령어를 입력해서 쓰는 **파이썬 프로그램**(CLI 도구)이야.
- 오픈소스 관리자가 "작업 기록"과 "점검 결과"를 문서로 남기도록 도와줘.
- 기능은 3개야.
  - `init`: 정해진 문서 파일들을 자동으로 만들어줌
  - `check`: 문서가 제대로 있는지, 글자 깨짐(한글 인코딩) 없는지 검사함
  - `evidence`: 점검 결과를 정리한 기록 파일을 만들어줌

### 개발 정보 (참고)

| 항목 | 값 |
| --- | --- |
| 언어 | Python (3.10 이상 필요) |
| 이 컴퓨터의 파이썬 | Python 3.12.3 |
| 추가로 설치할 외부 라이브러리 | 없음 (0개) |
| 패키지 이름 | `korean-agent-maintainer-kit` |
| 실행 명령어 | `agent-maintainer` 또는 `python3 -m agent_maintainer_kit` |

## 2. 무슨 일을 했어

### A. 설치

프로그램을 이 컴퓨터에서 실행할 수 있게 설치했어. 따로 받아야 할 다른 프로그램은 없었어.

```bash
python3 -m pip install -e . --break-system-packages
```

- `-e`(editable)는 "소스 코드를 고치면 다시 설치 안 해도 바로 반영되는" 방식이야. 개발할 때 편함.
- 설치하면 `agent-maintainer` 명령어가 `~/.local/bin` 폴더에 생기고, 이 폴더를 PATH(명령어를 찾는 경로)에 추가해뒀어.

### B. 자동 검사(테스트) 실행

개발자가 미리 만들어둔 검사 14개를 돌렸고 **전부 통과**했어.

```bash
python -m unittest discover -s tests
```

결과:

```
Ran 14 tests in 0.025s
OK
```

### C. 저장소 자체 점검 (CI와 동일한 방식)

이 프로젝트는 별도의 코드 검사기(린터)가 없고, 자기 자신을 검사하는 `check` 명령이 그 역할을 해. GitHub 자동 검사(CI)와 똑같은 명령을 돌려서 통과 확인했어.

```bash
python -m agent_maintainer_kit check . --no-fs-smoke
```

결과: `status: ok`, 검사한 파일 16개.

### D. 실제로 처음부터 끝까지 써보기 (핵심 기능 확인)

새 폴더를 하나 만들어서 프로그램의 3개 기능을 순서대로 실제로 실행해봤어.

```bash
# 1) 문서 만들기 (작업일지 파일명은 한글로)
agent-maintainer init /tmp/hello-repo --worklog-name 작업일지.md

# 2) 점검하기
agent-maintainer check /tmp/hello-repo

# 3) 기록 파일 만들기
agent-maintainer evidence /tmp/hello-repo --task "Add release checklist" \
  --command "python -m unittest discover -s tests" \
  --edge "Happy path: check passes"
```

결과:
- 문서 파일 6개 생성됨 (`AGENTS.md`, `작업일지.md`, `verification.md`, `human-touch-policy.md`, `CONTRIBUTING.md`, `SECURITY.md`)
- 점검 `status: ok`
- 기록 파일 `evidence/2026-08-01-add-release-checklist.md` 생성됨
- **한글 파일 이름(`작업일지.md`)도 문제없이 처리**되는 걸 확인 (이 도구의 핵심 목적 중 하나)

## 3. 앞으로를 위해 남겨둔 것

- **자동 설치 설정**: 다음에 이 컴퓨터(작업 환경)를 다시 켜면 위 설치 명령이 자동으로 실행되도록 등록해뒀어.
- **다음 작업자용 메모**: `AGENTS.md` 파일에 "이 프로젝트를 세팅·실행할 때 알아야 할 점"을 적어뒀어. (명령어 위치, 표준 명령 위치, 한글 검사 관련 주의점 등)
- **검토 요청(PR)**: 변경 내용을 PR로 올렸어. GitHub에서 승인·반영(merge)하면 돼.

## 4. 한눈에 요약

| 단계 | 명령어 | 결과 |
| --- | --- | --- |
| 테스트 | `python -m unittest discover -s tests` | 14개 통과 |
| 자체 점검 | `python -m agent_maintainer_kit check . --no-fs-smoke` | ok (16개 파일) |
| 실제 실행 | `init` → `check` → `evidence` | 문서 6개 생성, 점검 ok, 기록 파일 생성 |

결론: **설치 → 테스트 → 실제 실행**까지 전부 성공. 개발환경이 정상적으로 준비됐어.
